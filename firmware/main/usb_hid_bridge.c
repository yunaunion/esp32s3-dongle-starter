#include "usb_hid_bridge.h"

#include "esp_log.h"

static const char *TAG = "usb_hid";

esp_err_t usb_hid_bridge_init(void)
{
    ESP_LOGI(TAG, "USB HID bridge stub initialized");
    return ESP_OK;
}

